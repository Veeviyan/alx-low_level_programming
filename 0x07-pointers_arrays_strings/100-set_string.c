#include "main.h"

/**
 * set_string - function to be used
 * @s: pointer to the pointer pointing to the address of the character
 * @to: pointer that has the address of the variable
 * Return: address
 */

void set_string(char **s, char *to)
{
	*s = to;
}
